# Gmail Downloader Environment Variables

# Download configuration
DOWNLOAD_PATH=./downloads
SEARCH_QUERY=has:attachment
MAX_MESSAGES=100

# Gmail API configuration
GMAIL_CREDENTIALS_FILE=credentials/credentials.json

# File organization
CREATE_SUBJECT_FOLDERS=true

# Date filtering (YYYY/MM/DD format)
# DATE_FROM=2024/01/01
# DATE_TO=2024/12/31

# Advanced filtering (Regular Expressions)
# SUBJECT_REGEX=.*invoice.*
# FILENAME_REGEX=\.(pdf|xlsx?|docx?)$

# Examples of common filters:
# Filter by subject containing "invoice" or "receipt"
# SUBJECT_REGEX=.*(invoice|receipt).*

# Filter specific file types
# FILENAME_REGEX=\.(pdf|doc|docx|xls|xlsx)$

# Filter PDFs only
# FILENAME_REGEX=\.pdf$

# Filter files containing "statement" in filename
# FILENAME_REGEX=.*statement.*

# Optional: If using service account authentication
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/service-account.json

# Docker-specific paths (used in docker-compose.yml)
# DOWNLOAD_PATH=/app/downloads
# GMAIL_CREDENTIALS_FILE=/app/credentials/credentials.json
# CREATE_SUBJECT_FOLDERS=true