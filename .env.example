# Gmail Downloader Environment Variables

# Download configuration
DOWNLOAD_PATH=./downloads
SEARCH_QUERY=has:attachment
MAX_MESSAGES=100

# Gmail API configuration
GMAIL_CREDENTIALS_FILE=./credentials/credentials.json

# Optional: If using service account authentication
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/service-account.json

# Docker-specific paths (used in docker-compose.yml)
# DOWNLOAD_PATH=/app/downloads
# GMAIL_CREDENTIALS_FILE=/app/credentials/credentials.json